{
  "name": "sauce-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright UI tests for SauceDemo",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "@types/node": "^22.10.2"
  }
}
